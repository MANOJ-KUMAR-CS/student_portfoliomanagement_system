# Deployment Environment Variables
# Create a file named `.env` in this directory and copy these values for deployment

# Base URL for your Backend API
VITE_API_URL=http://localhost:5001
# Replace with the actual hosted backend URL (e.g., https://your-backend-app.herokuapp.com) inside your production .env 
